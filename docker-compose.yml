version: '3.0'
services:
  db:
    image: postgres:latest
    restart: always
    environment:
      - POSTGRES_USER=alexey
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5436:5432'
    volumes:
      - db:/var/lib/postgresql/data
volumes:
  db:
    driver: local
